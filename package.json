{
  "name": "h3tag-blockchain",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx nx run-many -t build --all --verbose",
    "test": "npx lerna run test",
    "clean": "npx lerna clean",
    "format": "npx lerna run format"
  },
  "devDependencies": {
    "@nx/eslint": "20.3.2",
    "@nx/jest": "20.3.2",
    "eslint": "~8.57.0",
    "jest": "^29.7.0",
    "lerna": "^8.1.9",
    "nx": "20.3.2"
  },
  "dependencies": {}
}
